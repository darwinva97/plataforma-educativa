<section class="cta-section">
  <div class="container cta-inner">
    <h2>Empieza tu viaje en programacion hoy</h2>
    <p>Unete a mas de 15,000 estudiantes que ya estan transformando su carrera. Tu primer curso es completamente gratis.</p>
    <div class="cta-buttons">
      <a href="/cursos" class="btn btn-primary btn-lg">Comienza Gratis Ahora</a>
      <a href="/#precios" class="btn btn-outline btn-lg">Ver Planes</a>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #1e40af 0%, #7c3aed 50%, #06b6d4 100%);
    position: relative;
    overflow: hidden;
  }
  .cta-section::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(rgba(255,255,255,0.08) 1px, transparent 1px);
    background-size: 24px 24px;
  }
  .cta-inner {
    text-align: center;
    position: relative;
    z-index: 1;
  }
  .cta-inner h2 {
    color: #fff;
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
  }
  .cta-inner p {
    color: rgba(255,255,255,0.85);
    font-size: 1.1rem;
    max-width: 550px;
    margin: 0 auto 2rem;
  }
  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
</style>
